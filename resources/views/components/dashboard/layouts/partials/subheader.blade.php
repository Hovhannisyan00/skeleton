<div class="subheader flex-wrap">
    <div class="subheader-left pt-2 pb-2">
        <h3 class="subheader-left-title">{{ __('__dashboard.'.($sub_header_data['pageName'] ?? '').'.title') }}</h3>
        <span class="subheader-left-separator"></span>

        <div class="subheader-breadcrumbs">

            <a href="#" class="breadcrumbs-link">{{ __('__dashboard.'.($sub_header_data['pageName'] ?? '').'.description') }}</a>
        </div>
    </div>
<!--    <div class="d-flex ml-auto">
        <a href="#" class="btn btn-secondary ml-2">{{ __('__dashboard.cancel') }}</a>
        <button type="submit" class="btn btn-primary ml-2">{{ __('__dashboard.save') }}</button>
    </div>-->
</div>
