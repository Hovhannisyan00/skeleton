/*
const options = {
  methods: {
    beforeSendRequest() {
      // Before send axios request function called
    },

    afterSuccess(resp) {
      // After success function called
    },

    afterError(resp) {
      // After error function called
    },
  }
}
*/

new FormRequest();
